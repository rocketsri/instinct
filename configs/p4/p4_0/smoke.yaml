experiment: p4_slack
seed: 401
seeds: [401, 402]
pilot_seeds: [401]
eval_seeds: [402]
preregistration:
  estimand: "Lower-tail quantile of exact replanning value above failure"
  primary_hypothesis: "A strict interior progress-recoverability frontier exists in the exact-label control"
  non_claim: "The exact-label construction is not a learned surrogate or a safety certificate"
theory: {proposal_version: "2.1", theory_id: "spec-7.1-7.6", amendment_id: "portfolio-2.1"}
compute: {run_class: smoke}
resume: {checkpoint_every_minutes: 15.0, allow_resume: true}
params: {q: 0.25, margin: 1.0, v_fail: -1.0, replan_horizon: 8}
