experiment: p4_slack
seed: 482
tag: p4.2-point-estimate-recovery1
notes: "Fresh layout; calibrated point recovery target instead of uniformly conservative LCB"
seeds: [482, 483, 484]
pilot_seeds: [482, 483]
eval_seeds: [484]
preregistration:
  estimand: "Held-out diagonal-impulse goal-probability gain of point-estimate slack chunks at matched replanning calls"
  primary_hypothesis: "Point-estimate slack chunks improve held-out success by at least one percentage point without more than five points of clean progress loss"
  non_claim: "An enumerated finite-state recovery is not learned robotics-scale P4.2, matched-wall-clock evidence, or a safety certificate"
theory:
  proposal_version: "2.1-p4.2-recovery1"
  theory_id: "spec-7.1-7.6"
  amendment_id: "portfolio-2.1"
compute: {run_class: smoke}
resume: {checkpoint_every_minutes: 15.0, allow_resume: true}
params:
  stage: p4.2
  policy_version: r1-point-estimate
  chunk_length: 3
  total_steps: 12
  beta: 0.1
  margin: 1.0
  v_fail: -1.0
  actuator_slip: 0.1
  value_horizon: 12
  progress_weight: 2.0
  minimum_success_gain: 0.01
  maximum_clean_progress_loss: 0.05
