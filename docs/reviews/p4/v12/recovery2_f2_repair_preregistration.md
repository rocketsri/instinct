# P4.2 recovery-2 F2 repair preregistration v12

The v10 recovery remains a STOP: it improved fresh held-out goal probability by
`0.050642` but its pilot-calibrated intervention rate transferred as `3.698005`
calls, outside the frozen `4.00 +/- 0.25` interval. No threshold is relaxed and
that evaluation unit is not reused.

Per the execution plan, estimator/instrumentation failures do not consume a
scientific recovery cycle. Amendment `p4-1.1` repairs only cost matching by
augmenting exact evaluation with a per-trajectory two-call verifier budget.
The prior pilot threshold (`1.996632239650217`), recovery estimator, policies,
minimum `0.01` gain, and total four-call target remain fixed. A new obstacle
layout is evaluated once under the same declared magnitude-two axial impulses.

This is not an amendment to the lower-quantile training estimand. A pass is
NARROW verifier evidence and archives slack training; a failure archives the
bounded P4.2 mechanism.
